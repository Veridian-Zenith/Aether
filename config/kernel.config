# Aether Linux kernel config
# Derived from CachyOS default — optimised for x86-64-v3
# Regenerate with: make localmodconfig && scripts/config --set-str CONFIG_LOCALVERSION "-aether"

# IMPORTANT: This is a placeholder. To generate the real config:
#   1. cd packages/sources/linux-6.14
#   2. zcat /proc/config.gz > .config
#   3. make olddefconfig
#   4. cp .config ../../../config/kernel.config
