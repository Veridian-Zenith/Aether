# ==== Extra Packages (BLFS — Beyond Linux From Scratch) ====
# Placeholder for future packages (desktop, server, etc.)
include("../cmake/PackageHelpers.cmake")

# define_package(
#     NAME        nginx
#     VERSION     1.26
#     URL         ...
#     STAGE       extra
# )
